{
  "name": "discord.js-v14-bot-template",
  "version": "1.0.0",
  "description": "Clone this repo to create a new discord.js v14 bot, no need to write boilerplate, everything's already set up",
  "main": "index.js",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "npx nodemon index.ts",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^20.5.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "discord.js": "^14.12.1",
    "dotenv": "^16.3.1"
  }
}
